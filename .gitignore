node_modules
dist
coverage
.turbo
.pnpm-store
.env
.env.*
!.env.example
docs/_site
docs/vendor
docs/.bundle
apps/web/dist
*.log
bin/
playwright-report/
test-results/
blob-report/
.worktrees/
.playwright-mcp/

env/*/data/

# Runtime data dir created when kaiad runs from this checkout
# (registry-auth keypair, builds workspace, etc.).
apps/api/data/

# Claude Code session state (lockfile, etc.) — never tracked.
.claude/

other-projects/