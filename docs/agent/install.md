---
title: Install Agent
nav_order: 2
---

# Install Agent

The v1 agent is written in Go and connects outbound over WSS.

1. Install binary or run container image.
2. Set `SM_REALTIME_URL`.
3. Start service (`systemd` unit included in `apps/agent/packaging/systemd`).
