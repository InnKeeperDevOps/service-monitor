{
  "name": "multiple-triggers",
  "description": "Two independent triggers (onCrash and onLogPattern) both feed into a shared shell action followed by a Slack notification.",
  "nodes": [
    { "id": "crash-trigger", "type": "onCrash" },
    { "id": "log-trigger", "type": "onLogPattern" },
    { "id": "shared-shell", "type": "runShell" },
    { "id": "notify", "type": "slackNotify" }
  ],
  "edges": [
    { "from": "crash-trigger", "to": "shared-shell" },
    { "from": "log-trigger", "to": "shared-shell" },
    { "from": "shared-shell", "to": "notify" }
  ],
  "expectedWaves": [["crash-trigger", "log-trigger"], ["shared-shell"], ["notify"]],
  "notes": "Both triggers are roots (in-degree 0) so they land in wave 0. The shared shell node waits for both triggers before executing."
}
