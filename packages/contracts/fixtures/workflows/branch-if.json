{
  "name": "branch-if",
  "description": "Trigger feeds into a branchIf node whose result determines which downstream path executes: shell (true) or email (false).",
  "nodes": [
    { "id": "trigger", "type": "onCrash" },
    { "id": "branch", "type": "branchIf" },
    { "id": "run-shell", "type": "runShell" },
    { "id": "send-email", "type": "emailNotify" }
  ],
  "edges": [
    { "from": "trigger", "to": "branch" },
    { "from": "branch", "to": "run-shell" },
    { "from": "branch", "to": "send-email" }
  ],
  "expectedWaves": [["trigger"], ["branch"], ["run-shell", "send-email"]],
  "notes": "Both branches appear in the same topological wave because they share the same depth. At runtime, the executor skips the branch not taken based on branchTaken ('true' → run-shell at index 0, 'false' → send-email at index 1)."
}
